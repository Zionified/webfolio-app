import Section from "@/components/Section"
import SectionAbout from "./SectionAbout"

const SectionAbouts = () => {
    return (
        <Section id="about">
            <Section.Title>ABOUT</Section.Title>
            <SectionAbout></SectionAbout>
        </Section>
    )
}

export default SectionAbouts
